import { ArticleList } from "@/components/bookmark/ArticleList";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <section>
      <ArticleList />
    </section>
  );
};

export default Home;
